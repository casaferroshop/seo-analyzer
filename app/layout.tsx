export const metadata = { title: "SEO Analyzer Pro", description: "Analiza páginas y sitemaps para SEO, schema y rendimiento" };
import './globals.css';
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (<html lang="es"><body>{children}</body></html>);
}